<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.qwk.mapper.HomeCarouselImageMapper">

    <resultMap type="HomeCarouselImage" id="HomeCarouselImageResult">
        <result property="id"    column="id"    />
        <result property="title"    column="title"    />
        <result property="url"    column="url"    />
        <result property="status"    column="status"    />
        <result property="sort"    column="sort"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectHomeCarouselImageVo">
        select id, title, url, status, sort, create_by, create_time, update_by, update_time from home_carousel_image
    </sql>

    <select id="selectHomeCarouselImageList" parameterType="HomeCarouselImage" resultMap="HomeCarouselImageResult">
        <include refid="selectHomeCarouselImageVo"/>
        <where>
            <if test="title != null  and title != ''"> and title = #{title}</if>
            <if test="url != null  and url != ''"> and url = #{url}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
            <if test="sort != null  and sort != ''"> and sort = #{sort}</if>
        </where>
        order by sort asc
    </select>

    <select id="selectHomeCarouselImageById" parameterType="Long" resultMap="HomeCarouselImageResult">
        <include refid="selectHomeCarouselImageVo"/>
        where id = #{id}
    </select>

    <insert id="insertHomeCarouselImage" parameterType="HomeCarouselImage" useGeneratedKeys="true" keyProperty="id">
        insert into home_carousel_image
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="title != null">title,</if>
            <if test="url != null">url,</if>
            <if test="status != null">status,</if>
            <if test="sort != null">sort,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="title != null">#{title},</if>
            <if test="url != null">#{url},</if>
            <if test="status != null">#{status},</if>
            <if test="sort != null">#{sort},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateHomeCarouselImage" parameterType="HomeCarouselImage">
        update home_carousel_image
        <trim prefix="SET" suffixOverrides=",">
            <if test="title != null">title = #{title},</if>
            <if test="url != null">url = #{url},</if>
            <if test="status != null">status = #{status},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteHomeCarouselImageById" parameterType="Long">
        delete from home_carousel_image where id = #{id}
    </delete>

    <delete id="deleteHomeCarouselImageByIds" parameterType="String">
        delete from home_carousel_image where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
