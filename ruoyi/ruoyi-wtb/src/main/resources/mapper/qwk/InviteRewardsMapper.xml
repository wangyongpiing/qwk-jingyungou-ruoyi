<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.qwk.mapper.InviteRewardsMapper">
    
    <resultMap type="InviteRewards" id="InviteRewardsResult">
        <result property="id"    column="id"    />
        <result property="userId"    column="user_id"    />
        <result property="userName"    column="user_name"    />
        <result property="income"    column="income"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <sql id="selectInviteRewardsVo">
        select id, user_id, user_name, income, create_by, create_time, update_by, update_time, remark from invite_rewards
    </sql>

    <select id="selectInviteRewardsList" parameterType="InviteRewards" resultMap="InviteRewardsResult">
        <include refid="selectInviteRewardsVo"/>
        <where>  
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="userName != null  and userName != ''"> and user_name like concat('%', #{userName}, '%')</if>
            <if test="income != null "> and income = #{income}</if>
        </where>
    </select>
    
    <select id="selectInviteRewardsById" parameterType="Long" resultMap="InviteRewardsResult">
        <include refid="selectInviteRewardsVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertInviteRewards" parameterType="InviteRewards">
        insert into invite_rewards
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="userId != null">user_id,</if>
            <if test="userName != null">user_name,</if>
            <if test="income != null">income,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="userId != null">#{userId},</if>
            <if test="userName != null">#{userName},</if>
            <if test="income != null">#{income},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateInviteRewards" parameterType="InviteRewards">
        update invite_rewards
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="userName != null">user_name = #{userName},</if>
            <if test="income != null">income = #{income},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteInviteRewardsById" parameterType="Long">
        delete from invite_rewards where id = #{id}
    </delete>

    <delete id="deleteInviteRewardsByIds" parameterType="String">
        delete from invite_rewards where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectByUserId" parameterType="long" resultMap="InviteRewardsResult">
        <include refid="selectInviteRewardsVo"/>
        where user_id = #{userId}
    </select>

</mapper>